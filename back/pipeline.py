from pipeline import obtener_texto_de_blob_pdf
import cpp_backend


def obtener_texto_de_blob_pdf
def split_text:

def get_embedding:


def cluster_cuda:

# Una funcion que reciba un pdf y str,  devuelve respuesta (str)
   # extraer texto por utils(azure DocumentIntelligenceClient) def obtener_texto_de_blob_pdf
   # hace split al texto obtenido del con la funcion split_text
   # convierte los texto a embbeding y los guarda en la cache del dispositivo
   # toma el str del inicio (el que se envia desde el fronrted el mensaje) lo convierte a embedding
   #  utiliza la funcion cluster para obtener los vectores mas cercanos con kmeans
   # reconstruye los vectores a informacion y lo pasa a pHi-3 para humanizarlo
   # retorna esa informacion string 